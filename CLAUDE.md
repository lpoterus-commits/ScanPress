# ScanPress —— 扫描图片转 PDF(纯影印,无 AI)

> **新会话来维护本项目,先读 [`维护交接.md`](维护交接.md)**:那里有代码地图、构建/验证的最小闭环、
> 「哪些参数是用户实测定下的、不能随手改」、以及已经试过并否掉的路线。本文件是它的背书材料
> ——全部实测数据与踩坑经过都在这里。

## 项目性质
把扫描仪(CZUR 等)拍的整本书图片,做成**高清晰、小体积**的 PDF。全程确定性、无 AI、无 OCR、无网络,
用户自己就能跑。交付物只有 PDF:**不可搜索、不可选取复制、无目录书签**,须向用户明确。

需要「可搜索 PDF + Word」的书籍转录流水线是**另一个项目**(作者本机的私有项目,未开源,
那边靠 Claude 视觉逐页转录)。两者共用二值化配方(阈值 55% + 边缘涂白)与切段 jbig2 的思路,
一边改了另一边留意同步。

## 目录结构
```
ScanPress/
  维护交接.md            ★ 维护本项目从这里开始
  CLAUDE.md              实测数据与踩坑(本文件)
  scripts/scan2pdf.py     引擎:图片 → PDF(全部转换逻辑都在这里,零配置、可单独用)
  scripts/merge_pdfs.py   合并多个 PDF(pikepdf 搬运页面对象,零损失)
  app/ScanToPDF.swift     原生 SwiftUI 图形外壳(两个标签页:图片转 PDF / 合并 PDF)
  app/build_app.py        一条命令构建 .app 并装到 ~/Applications(顶部 VERSION 常量定版本与应用名)
  app/README_安装说明.txt  随 DMG 分发的说明
  app/WINDOWS_移植交接.md  Windows 版移植交接文档(方案 C:单独做一个 Windows 界面)
  app/dist/               打包产物 .dmg / .zip
```
构建并安装:`python3 app/build_app.py`(只需 Xcode Command Line Tools,不需完整 Xcode)。
`build_app.py` 按 `../scripts/` 找引擎脚本并复制进 .app,**改完脚本必须重跑它**,否则应用里还是旧引擎。

## 依赖内嵌进 .app(2026-08-03)
`build_app.py` 的 `bundle_tools()` 把 magick / jbig2 / jbig2topdf.py 连同 **127 个 coder 模块 + 17 个 dylib**
(15 MB,包总大小 20 MB)搬进包内,`install_name_tool` 把依赖路径改写成 `@loader_path` 相对引用,
运行时由 `ScanToPDF.swift` 的 `enum Tools` 设 `PATH` / `MAGICK_CONFIGURE_PATH` /
`MAGICK_CODER_MODULE_PATH` / `MAGICK_FILTER_MODULE_PATH` 指过去。**GUI 从此不需要 homebrew**
(命令行直接跑 scan2pdf.py 仍需要,它走 PATH)。**Python 环境 pdfenv 尚未内嵌,仍是外部依赖。**
排错过程中确认的四件事(详见 `维护交接.md` 第 5 节):
- **这个 homebrew 版 ImageMagick 是模块化编译**(目录名 `lib/ImageMagick/modules-Q16HDRI/`,不带 `-7`),
  不搬模块则连 JPEG 都读不了(`no decode delegate`)。
- **libltdl 认 `.la` 不认 `.so`**,且 `.la` 里的 `libdir=` 绝对路径会把 homebrew 那份 `.so` 拽回来加载
  ——**必须清空成 `libdir=''`**,否则本机测试全绿、换机器就崩。最阴的一个坑。
- **`heic.so` → libheif → x265 是 GPL-2.0**,不能进 MIT 应用;`TAINTED_LIB_PAT` 按依赖闭包自动排除
  (实测只命中 heic 一个,顺带省 13 MB)。potrace 同理,永不内嵌。
- 带执行位的 `.py` 放 `Contents/Helpers/` 会被 codesign 当未签名的嵌套代码,故 `jbig2topdf.py` 放 `Resources/`。

**验证方法**(唯一可信的):`env -i PATH=<Helpers>:/usr/bin:/bin` 跑真实转换,再与 homebrew 版逐图像流比 md5。
bw / mrc 两模式各 20 页实测**逐字节一致**——内嵌没有改变任何输出。

## 环境依赖(homebrew 工具链,GUI 启动时自检)
- `/opt/homebrew/bin`:`magick`(ImageMagick 7)、`jbig2`(jbig2enc)+ `jbig2topdf.py`、`potrace`(仅矢量模式)
- **pdfenv**:`~/.venvs/pdfenv`(pikepdf / img2pdf / Pillow)。重建:
  ```
  python3 -m venv ~/.venvs/pdfenv && ~/.venvs/pdfenv/bin/pip install pikepdf reportlab img2pdf
  ```
- **macOS 系统 bash 是 3.2**,编排逻辑一律用 python,别写非平凡 shell 脚本。

## 用法与全部实测经验
用户只要「高清晰 + 最小体积的图片 PDF」时走这条路 —— 去掉转录后全程确定性,**用户可自己手动跑**,不需要 Claude:
```bash
~/.venvs/pdfenv/bin/python scripts/scan2pdf.py <源图目录> <输出.pdf> --width 0 --covers 1,2 --title "书名"
```
- 零配置(不读 book.json),自动自然排序 / 自动识别 .jpg 实为 TIFF / 断点续做(中间产物在 `~/.cache/scan2pdf/<源目录名>`)。
- 三模式:`bw`(默认,二值化+JBIG2,复用主流程的阈值 55% + 边缘涂白配方)、`gray`、`color`;`--covers` 指定保留彩色的页(默认首末,自然法式封底在 image00002 就写 `1,2`)。统一 A4 内置(pikepdf 矢量缩放,与 unify_a4 同法)。
- `--probe` 抽样 8 页 × 多档位,打印 KB/页 + 全书预估 + 导出样张 PDF,**定档不用全书重跑**。
- **⚡ 切段并行 jbig2(2026-07-30,最大的一次提速)**:jbig2 的符号词典随页数膨胀,**每页成本超线性增长**——848 页整本压 888s(1.05 s/页),切 8 段(每段 106 页)**顺序**跑只要 77s(**11.6×,零并行**),8 段并行 26-30s(**29.5×**);114 页的书 40s→**2s**,全流程 58s→8.4s。体积代价:848 页 +2.0%、114 页 +1.0%。**关键认知:收益主要来自「词典变小」(11.6×)而非并行(再 2.6×)**,故**任何情况下都切段**,只有「同时压几段」跟着可用核数走。实现见 `jbig2_pdf()`:`k = min(8, 页数//16)` 段 → ThreadPoolExecutor 并行 → pikepdf 按序拼(二值化像素一位不改)。**并行度用跨进程全局名额自适应**(`jbig2_slot()`:`~/.cache/scan2pdf/.slots/` 下 8 个 flock 文件,所有 scan2pdf 进程共抢):单任务独跑时独占 8 个(实测采样恒为 8),中途再加任务则自动分摊、**无需重启已在跑的任务**;flock 在进程死亡时由内核释放,不会死锁。比「启动时按并发数算死」正确得多——后者既不会给单任务开满,也无法响应中途变化。**回归验证方法**:切段版与单段版的 content.pdf 逐页渲染比对——页序完全一致、文字内容一致,像素差异 0.011-0.023%(**JBIG2 `-s` 符号模式本身有损**,词典不同→代表字形略不同,属固有特性而非 bug)。
- **中间产物自动清理**:成功写出成品后立刻删掉该档位的二值化 PNG 与 content.pdf(`--keep-cache` 可保留);**失败/中断时一律保留**以便断点续做。此前只增不减,实测用户机器上已攒 465 MB。GUI 底部显示残留大小 + 「清理」按钮。
- **坑:jbig2/leptonica 会重映射 `/tmp/` 开头的绝对路径**(报 `fopenReadStream: failed to open locally with tail ...`,文件明明存在)。调 jbig2 一律 `cwd=工作目录` + 相对路径,勿传 /tmp 绝对路径。
- **实测档位(《自然法》116 页,2026-07-30)**:bw 原生 2646px=40 KB/页(4.7 MB),bw 1800px=28 KB/页(3.3 MB),bw 1400px=20 KB/页;**gray 1800px/q45=338 KB/页**(纸张纹理使 JPEG 失效,比二值化差 10 倍)。
- **清晰度不够时怎么办(2026-07-30 实测,《自然法》image00060 逐档放大对比)**:原生分辨率就是物理上限,插值放大只让笔画变肿、不增细节;bw 模式的观感损失主要来自二值化丢掉抗锯齿灰边,但 320 DPI 下阈值 55% 的笔画形状已贴着灰度原图走,黑白模式内部余量很小。可用手段按性价比:(a) **阈值微调**(probe 已能出 50/55/60 样张,墨淡的书调到 50 捞回细笔画);(b) **`--enhance` 提白+锐化预处理**(`-level 15%,88% -unsharp 0x1.2+1.2+0.01`,施加于 resize 之后、threshold 之前;开启时阈值默认自动 55%→58%,因提白会推亮整体)——实测单页 51→52 KB、全书 4.71→4.8 MB(**+2%**),**打光均匀的扫描肉眼几乎无差别,价值在墨淡/发灰的书**;(c) **局部自适应阈值 `-lat` 是死路**(实测 `-lat 60x60+12%` 糊肿填死字腔且体积翻倍到 99 KB/页,`-lat 100x100+18%` 直接把字抹掉只剩 8 KB/页)——CZUR 打光均匀、底噪低,全局阈值本就够用,`-lat` 只对光照不均/书脊阴影重的页有意义且须逐书调参,**不要默认开**;(d) 真要保住灰边只能走 gray 模式(体积 ~10x);(e) 若原图本身虚焦/手抖,软件端无解,回去重扫。
- **结论:bw 模式一律用 `--width 0`(原生分辨率)**。二值化后分辨率的体积代价极小(+35%,绝对值几 MB),而放大 3 倍看笔画边缘明显更实;此处与踩坑24(j)「适中分辨率+强锐化胜过高分辨率」**不冲突也不适用**——那条的前提是有锐化补偿,纯影印无任何增强,清晰度只能靠像素。
- 交付物只有 PDF:**不可搜索、不可选取复制、无目录书签**,须向用户明确。
- **四种画质模式(2026-07-31 扩充,均以 15 页韩语考卷实测)**:`bw` 二值化+JBIG2(0.34 MB)、`smooth` 平滑黑白(抠选区→形态学平滑轮廓→降采样抗锯齿→3级灰/2bit PNG,0.55–2.1 MB)、`vector` potrace 矢量描摹(2.7 MB,**分辨率无关、放大无极限**,182 KB/页,大部头不划算)、`gray`/`color`(3.6–5.3 MB)。**用户实测选定的组合:`bw --enhance --supersample 2`(0.63 MB)——原生分辨率二值化 + 提白锐化 + 2 倍超采样,"既清晰体积也不大",定为**几十页文稿**的推荐档**。**⚠ 大部头必须关掉 `--supersample`**:《교회법 해설 6》1506 页实测 22 KB/页(纯黑白,与主线流程旧版 33.4 MB 吻合)→ 41 KB/页(开超采样),全书 32 MB → 62 MB;而 `--enhance` 几乎不占体积(22→23 KB/页)。GUI 已加提醒:图片数 ≥300 且勾了边缘精细化时,「输出设置」顶部弹橙色提示 +「关掉」按钮。要点:(a) 锐化**不能**减轻二值化锯齿(实测粗糙度反而 +0.8%),锯齿是 1-bit 量化的必然结果,只能靠「保留灰阶」或「缩小像素(超采样)」解决;(b) `--supersample 2` 阈值前放大 2 倍,台阶细一半、体积 ×2;(c) Photoshop 的"平滑"实为**灰度+色阶**而非位图模式,其扩散仿色对高对比扫描件无改善(实测 14 vs 13 KB/页、观感相同);(d) potrace 参数:**必须 2x 位图再描摹**——形态学平滑按像素计,在 1x 原生位图上 `Diamond:2` 会把笔画啃断成碎块(2026-07-31 实测事故,产出不可读),`-O 1.0 -t 10` 丢弃 ≤10 像素杂点;**用户看过矢量版后明确否掉**(体积 2.7 MB 却不比 `bw --enhance --supersample 2` 的 0.63 MB 好),故 GUI 不提供该模式,仅保留命令行 `--mode vector`。
- **彩色模式的提白锐化(2026-07-31)**:`--enhance` 按模式自动换配方——黑白/平滑黑白用激进档(`-level 15%,88% -unsharp 0x1.2+1.2+0.01`,为"阈值切得准"调的),彩色/灰度用主线流程验证过的温和档(`-level 6%,94% -sigmoidal-contrast 3.5,50% -modulate 102,107,100 -unsharp 0x0.9+1.0+0.008`,灰度跳过 modulate)。**不可混用**:黑白档施于彩图会把浅色压掉(纯白像素 3.4%→11.2%,彩色配方只到 8.5%)。《퀴즈 과학 상식》198 页 900px/q48 实测:原样 28.8 MB → 增强 35.7 MB(+24%),与主线流程当年那版 35 MB 吻合。
- **MRC 混合分层 = `--mode mrc`,v2.0 正式模式(2026-07-31,用户验收通过)**:一页拆两层——①彩色底图大幅降分辨率(700px/q45)只管颜色 ②1-bit 文字蒙版走 JBIG2、**保持原生 2646px** 只管"哪里是墨",PDF 里底图打底 + `/ImageMask` стencil 用纯黑叠上。**关键坑**:(a) 蒙版不能只按明暗取,否则红头发/粉蝴蝶结这类"暗但鲜艳"的区域被涂黑——必须 **(够暗) AND (低饱和)** 双条件(HSL 的 S 通道阈值),实测暗 45%/饱 25% 最平衡(暗58/饱35 会把淡绿网点也吃进去);(b) 再加 `-morphology Open Diamond:1` 去孤立杂点;(c) jbig2enc 输出里 0=黑,ImageMask 默认 Decode[0 1] 即 0 处上色,**不要设 Decode[1 0]**(会整页涂黑);(d) 封面封底不做 MRC(纯彩色高分辨率),否则深色画面被蒙版涂黑。**同等文字锐度下体积对比**(6 页实测):普通彩色 900px=165 KB/页(文字仅 900px)、1300px=263、1500px=318,而 MRC 底图700px=170 KB/页而**文字是 2646px 原生**。
  **定案档位(用户实测选定,即 CLI 默认值)**:`--mrc-dark 50% --mrc-chroma 35% --mrc-mask-width 2200 --mrc-bg-width 500 --mrc-bg-quality 35` → 《퀴즈 과학 상식》198 页 **14.2 MB**(普通彩色增强版 35.7 MB,即**体积减 60% 而文字分辨率从 900px 升到 2200px**)。层占比:蒙版 JBIG2 58%、底图 JPEG 42%。
  **六轮排错的完整教训(改参数前必读,顺序即发现顺序)**:(1) 只按明暗取墨 → 红头发/粉蝴蝶结被涂黑,必须加色度门;(2) 色度门用 HSL 饱和度 → **近黑像素饱和度因分母趋零而虚高**,黑笔画内部被误排除只剩空心轮廓,必须改用 **HCL 的 C 通道(绝对色度)**;(3) 暗度门 65% 太松 → 中性灰画面(路面)被当墨出黑斑,收到 50% 干净;(4) 底图保留文字 → 锐字周围一圈模糊鬼影+JPEG 振铃(用户报"斑点"),必须先挖墨;(5) 挖墨用"整幅模糊"填 → 模糊图含墨、糊出脏色被 4:2:0 色度采样拉成**蓝紫横条拖影**;(6) 挖洞时把墨区 Dilate 放大 → 洞比字大,黑字盖不住,彩色区露白边;**正解是按蒙版原样设透明 + alpha 加权缩放 + 剩余空洞填白**;(7) 色度门 20% 太严 → **本书标题是深蓝色**,笔画被切碎、露出底图模糊蓝字(用户报"最后三个字有问题"),35% 时标题完整而人物不被涂黑,55% 开始误伤人物。**副作用:深色彩色文字会被画成纯黑**(蒙版只能单色填充),要保留原色需做双色分层,用户接受变黑。
  **彩色文字多的材料要关掉彩色分层(2026-08-01,ScanPress 2.1,180 页韩语教材《구해줘》)**:漫画绘本是「黑字为主 + 彩色插图」,MRC 的彩色分层(按色相聚类,黑字层 + 最多 3 个彩色字层)很合适;但**彩色文字密集的教材/杂志完全相反**——用户报「彩色字普遍地有'残缺',像是被虫子啃了一样」。根因:同一个彩色字的笔画会被**劈成两半**,一部分进了锐利的蒙版层、一部分留在模糊的低分辨率底图上,拼在一起就是啃噬状缺口;这是分层判定的**边界必然**(任何阈值都在笔画的抗锯齿过渡带上切),靠 Close/Dilate 形态学修补只能微弱缓解。**正解是 `--mrc-no-color-layers`:只做黑字层,彩色文字整个交给底图,同时把底图提到 1500px/q50**——彩字不再有拼接感、观感均匀,黑字仍是 2600px 原生。实测 180 页 **17.4 MB / 214 s**(全彩增强版 38 MB),用户验收「文字变清了很多」。故 **GUI 给「材料类型」预设**:教材/杂志档 = 蒙版2600 + 底图1500/q50 + 关分层(**新默认**),漫画/绘本档 = 蒙版2200 + 底图500/q35 + 开分层。**排错中另外三个坑**:(a) ImageMagick 的 `minus` 是**目标减源**(第一张是目标),顺序反了蒙版全空;(b) `-resize NxN>` 只缩不放,彩色层在 1600px 检测后不会被放大到蒙版尺寸,要写 `NxN`(无 `>`);(c) 尺寸不一致的图 `-layers flatten` 会把并集蒙版撑到 99.6%(底图被挖光,插图整个消失),合并前必须 `-resize WxH!` 对齐。**已试过并否掉的路线**:改用「结构检测」(顶帽/底帽提取细笔画,与颜色无关)取代颜色判定——文字确实全抓到了,但对结构蒙版内像素做 RGB k-means 只聚出中性簇,**深红文字被画成灰色**,且体积翻倍到 132 KB/页,不如直接关分层。
- **GUI 档位设置跨启动记忆**(UserDefaults,`local.scan2pdf`):mode/width/quality/threshold/enhance/supersample/coverPreset/mrcPreset/mrcColorLayers/mrcMaskWidth/mrcBgWidth/mrcBgQuality。**坑:带 `didSet` 的预设属性会在启动恢复设置时反手覆盖用户微调过的数值**——预设 `didSet` 里要 `guard loaded else { return }`,且判断「是否有旧设置」用**新加的那个键**(如 `mrcPreset`)而非老键,否则从旧版本升上来的用户会拿到「预设标签是新档、数值还是旧档」的错位状态。
- **推荐档的耗时剖析(2026-07-31,116 页《自然法》原生 `--enhance --supersample 2`)**:全程 **32–40 秒**(同配置连跑波动 ±15%,属机器噪声),其中**二值化占 94%、jbig2 仅 2 秒**(切段并行后压缩已不再是瓶颈)。单页拆解:全套 1372ms = 基础阈值/IO 261ms + **2 倍超采样 806ms(59%)** + 锐化 234ms。已验证**无效**的优化:`MAGICK_THREAD_LIMIT=1`(无变化,非线程争抢)、PNG 压缩级别(仅 74ms)、PBM 中间格式(反而慢 2.7 倍)、`-filter Triangle`(-4%)、百分比 `-shave`(反而慢)。已采纳:**用 Pillow 读宽高替代每页一次 `magick identify`**(省 43ms/页的进程开销,千页书 ≈45 秒;单本小书埋在噪声里但对大部头实打实)。**剩余唯一的实质杠杆是超采样倍数**:1.5 倍 951ms/页(比 2 倍快 31%,台阶略粗于 2 倍但仍细于 1 倍)——不改则维持现状,2 倍是画质优先的选择。
- **合并多个 PDF**:`scripts/merge_pdfs.py`(pikepdf 搬运页面对象,JBIG2/JPEG 流原样带过去,**画质与压缩零损失**,体积≈各输入之和;可选 `--a4` 只对非 A4 页做矢量缩放、`--bookmarks` 每个输入建一个书签、`--title`)。用法 `~/.venvs/pdfenv/bin/python scripts/merge_pdfs.py <输出.pdf> <输入1> <输入2> ...`;实测 4.7+4.8+1.7 MB → 11.3 MB/237 页,书签与 A4 均正确。**绝不能用「打印成 PDF」类工具合并**(重新光栅化,毁压缩+文字层,同踩坑6)。
- **图形界面版**:`app/ScanToPDF.swift`(原生 SwiftUI,两个标签页:图片转 PDF / 合并 PDF;应用名「（自制）扫描转PDF」)。**界面用 `Form + .formStyle(.grouped)` 分组卡片**(macOS 13+,系统设置同款观感)+ SF Symbols 图标 + 状态胶囊;长说明改挂 `.help()` 悬停提示,不再堆小字。两个坑:`Section(isExpanded:)` 要 macOS 14,13 上用 `Section { DisclosureGroup {} }` 替代;**拼接字符串里的 Markdown(`**粗体**`)不会被解析**,只有整段字面量才解析,否则星号会原样显示+ `app/build_app.py`(swiftc 编译 → 组装 .app → magick/iconutil 生成图标 → codesign -s - 临时签名 → 装到 ~/Applications)。`python3 app/build_app.py` 一条命令重建(顶部 `VERSION` 常量同时决定应用名与 Info.plist 版本,安装时自动删除同族旧版本);**打包**:`app/dist/` 下的 .dmg/.zip(内含 README_安装说明.txt + /Applications 符号链接),由 hdiutil UDZO 制作,约 1.2 MB;GUI 只是外壳,转换逻辑全在 scan2pdf.py(随构建复制进 Contents/Resources),靠 `--progress` 输出的 `@@P/@@S/@@DONE` 行驱动进度条。**只需 Xcode Command Line Tools,不需完整 Xcode**;两个坑:(a) swiftc 默认按 top-level code 解析,必须 `-parse-as-library`(否则 `@main` 报错),并用 `-swift-version 5` 规避 Swift 6 并发严格检查;(b) 本机 magick 无默认字体,`-annotate` 必须显式 `-font /System/Library/Fonts/*.ttc`。应用启动时自检 pdfenv/magick/jbig2/jbig2topdf.py,缺则橙色面板提示装法(未做依赖内嵌,故只能在装好 homebrew 工具链的机器上跑)。**GUI 曾有静默覆盖 bug(2026-07-30 修)**:输出路径在选源文件夹时算定为 `<文件夹名>.pdf`,而「PDF 标题」只写 `dc:title` 元数据、不影响文件名 —— 用户改档位+改标题重跑,第二份直接把第一份盖掉。修法三处:输出行改为**可直接编辑的文件名** TextField(+「换位置…」)、标题行加「不会改文件名」说明、start() 前 `resolveOverwrite()` 弹「自动改名/覆盖/取消」(自动改名追加 ` 2`/` 3`)。**给 GUI 加任何写文件路径时,一律先查存在性再问**。**文件名输入框必须净化**(2026-07-30 实例):用户文件名里出现 `could not?create image?from?could not?create image?from?` —— 查证是 `screencapture` 的报错文本(`strings /usr/sbin/screencapture` 可见三条 `could not create image from display/rect/window`),且当时**就躺在剪贴板里**(`osascript -e 'the clipboard as text'` 证实),即某次「截图到剪贴板」失败后错误文本代替图片进了剪贴板,用户 Cmd+V 到文件名栏。修法:共用 `sanitizeName()` 去控制字符/换行/`/`/`:`/`\`+trim+截断 120 字,`nameBinding()` 供两个标签页共用。**排查此类「文件名/输入框里冒出莫名文字」,第一步查剪贴板**。**任务队列 + 并发(2026-07-30 加)**:GUI 的「开始转换」改为「加入队列」,`JobQueue.shared` 管并发(可设 1–4,**默认 3**),每个 `Job` 独立 Process/进度/日志,同源同档位不许排两次(缓存目录按「源目录+档位」分,同档位并发会踩产物),全部跑完 `NSSound.beep()`。**并发依据的实测(M5/10核=4P+6E,24GB,《自然法》114页原生)**:二值化 6s(吃满多核)+ **JBIG2 49s(单线程,只占 1 核)** + 组装 3s = 58s,即 **85% 时间卡在单线程 jbig2**,并行做多本能把闲置核用起来。吞吐实测:**并发1=1.0 本/分、并发3=2.2 本/分(83s 跑完3本 vs 串行175s,2.1倍)、并发4=2.0 本/分(反降,只有4个性能核,第4个 jbig2 落到能效核拖慢全局)**——故默认 3、上限 4。每任务内部 `--jobs = max(2, 核数/并发数)` 避免二值化阶段过度订阅。资源:jbig2 峰值 ≈ 2.2 MB/页(1250页大部头≈2.7GB,并发3约8GB,24GB 够用),磁盘缓存 ≈ 0.1 MB/页。另:缓存目录 `~/.cache/scan2pdf/<源目录名>/<mode>_<width>_<threshold|quality>[_e]/` 按档位分开,故换回旧档位重跑只需重组 PDF(秒级),不必重新二值化。
