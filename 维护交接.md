# ScanPress 维护交接

写给「以后开新会话来改这个软件」的自己。**先读本文,再读 `CLAUDE.md`**:
本文讲**怎么动手**(代码在哪、怎么构建验证、哪些不能动),`CLAUDE.md` 讲**为什么是现在这样**(全部实测数据与踩坑)。

---

## 1. 现状(2026-08-03)

| 项 | 值 |
|---|---|
| 版本 | **2.2**(应用名 `ScanPress 2.2`,版本号在 `app/build_app.py` 顶部 `VERSION` 常量,一处改全处生效) |
| 依赖内嵌 | **全部内嵌,GUI 已自包含**(2026-08-03):magick / jbig2 / jbig2topdf.py + 127 个 coder 模块 + 17 个 dylib(15 MB),以及带 pikepdf/img2pdf/Pillow 的 CPython 3.14.6(107 MB)。**包总大小 128 MB**。命令行直接跑 scan2pdf.py 仍需 homebrew + pdfenv |
| 已安装 | `~/Applications/ScanPress 2.2.app` |
| 分发包 | `app/dist/ScanPress 2.2.dmg`、`.zip`(自包含后约 50 MB) |
| Windows 版 | **未做**。交接文档 `app/WINDOWS_移植交接.md` + `app/dist/ScanPress-Windows移植包.zip`,方案是「在 Windows 上单独写一个界面、复用同一份 scan2pdf.py」 |
| 用户 | 就是本仓库主人。用扫描仪(CZUR)拍整本书,要**高清晰 + 小体积**的 PDF。日常从图形界面用,极少用命令行 |

**用户对当前效果是满意的**(2026-08-03 原话:「文字变清了很多,似乎是够用的。我先这么用着,后期若遇到新的难题再找你调整。」)。
所以默认心态是:**别主动优化画质参数**,等用户报问题再动。

---

## 2. 三分钟上手(改代码 → 构建 → 验证)

```bash
cd <仓库根目录>
# 改 scripts/scan2pdf.py 或 app/ScanToPDF.swift 之后:
pkill -x ScanToPDF          # 应用在跑的话先退,否则覆盖安装不干净
python3 app/build_app.py    # 编译 swift → 组装 .app → 复制脚本 → 签名 → 装到 ~/Applications
```

**最容易犯的错:改了 `scripts/scan2pdf.py` 却没重跑 `build_app.py`。** 应用跑的是 `.app` 包内的拷贝,
源文件改了它一无所知。每次改完脚本用这条确认包内外一致:

```bash
md5 -q scripts/scan2pdf.py "$HOME/Applications/ScanPress 2.2.app/Contents/Resources/scan2pdf.py"
```

只测引擎不用构建应用:

```bash
~/.venvs/pdfenv/bin/python scripts/scan2pdf.py <源图目录> /tmp/out.pdf --mode bw --enhance --supersample 2
```

`--probe` 可以只抽 8 页试多个档位,**定档不必全书重跑**——调参时永远先用它。

---

## 3. 代码地图

### `scripts/scan2pdf.py`(890 行,全部转换逻辑;零配置、不读 book.json)

| 位置 | 职责 | 备注 |
|---|---|---|
| `emit/die/run/fresh` | 进度输出、报错、跑外部命令 | `emit` 就是 `@@` 协议的出口 |
| `parse_ranges` `list_images` `img_size` | 选页、自然排序、读宽高 | `img_size` 用 Pillow,**别改回 `magick identify`**(每页贵 43 ms) |
| `binarize` (132) | bw 模式:缩放 → 增强 → 超采样 → 阈值 → 边缘涂白 | 边缘涂白 = 左右各 1.5%、上下各 1%,去书脊阴影 |
| `smooth_bw` (155) | smooth 模式:4× 阈值 → 形态学平滑 → 降采样 → 3 级灰 | **GUI 不暴露**;用户实测觉得比 bw 更糊,故没进界面 |
| `vectorize` (184) | vector 模式:potrace 描摹 | **GUI 不暴露**,仅命令行;`zoom` 必须 ≥2 |
| `mrc_layers` (228) | MRC 拆层:黑字蒙版 / 彩色字蒙版 / 挖墨后的彩色底图 | **全文件最难的函数**,改前必读函数内注释块 |
| `mrc_compose` (390) | 把各层压 JBIG2 并组成 PDF 页 | 每层一个 JBIG2 批次以共享词典 |
| `to_jpeg` (440) | gray/color 模式出图 | 增强配方按模式分档 |
| `pmap` (454) | 多进程跑单页任务 | |
| `jbig2_slot` (473) | **跨进程 flock 名额**(`~/.cache/scan2pdf/.slots/`,8 个) | 单任务独占 8 个,中途加任务自动分摊,无需重启 |
| `jbig2_pdf` (501) | **切段并行 JBIG2** | 切段本身就是 11.6× 提速(词典变小),并行只再给 2.6×,故**任何情况下都切段** |
| `assemble` (564) | 组页 + 统一 A4(pikepdf 矢量缩放) | |
| `build` (591) | 一次转换的总编排 | 断点续做、缓存清理都在这 |
| `main` (739) | 参数解析 | |

### `scripts/shrink_pdf.py`(已有 PDF 瘦身,2026-08-03 新增)

**无损重压**:把 PDF 里 1 位黑白图的 CCITT G4 编码换成 **JBIG2 通用编码**,位图一比特不变。
实测四本自扫韩语书 354.8 MB → 148.6 MB(**省 58%**,80 秒),全书逐像素比对零差异。
`--analyze` 只体检不写文件(抽样真压 10 页来估,不用经验系数)。

**三个不能动的地方**:
1. **必须用通用编码,不能用符号模式**(`-s`)。符号模式靠字形聚类省体积,是有损的;
   而且实测在带扫描噪声的原图上**反而比通用编码大 19%**——噪声让同字每次像素都不同,词典失效。
2. **`pil.load()` 不能删。** 不强制完整解码就 save,损坏的 CCITT 流会产出残缺位图:
   实测某书第 3、4 页因此各差了 140 万像素,而且**不报错**,是靠全书逐像素比对才发现的。
3. **`capture_cstderr()` 是最后一道保险**,接住 libtiff 在 fd 2 上的抱怨(Python 的
   warnings 捕获不到)。它**必须串行调用**——fd 重定向是进程全局的,多线程会串音;
   所以流程是「解码串行 → jbig2 编码并行」。

### `scripts/ocr_pdf.py` + `app/SPHelper.swift`(不可见文字层,2026-08-03 新增)

给影印 PDF 加可搜索的隐形文字层,**页面外观逐像素不变**(渲染模式 `3 Tr`)。
用 **macOS 自带 Vision**,不是 Tesseract:同页韩语实测 Vision 0.56 秒且标题正文全对,
Tesseract 1.39 秒、漏标题、错字、吐垃圾。实测 20 页识别 445 行、每页只增 1.2 KB,
PDFKit(即 Preview 的 Cmd+F)搜韩文全部命中。

- `SPHelper.swift` 编译成 `Contents/Helpers/sphelper`,子命令:`ocrpage`(PDF 某页渲染+识别
  **一步到位,不落地 PNG**——ocr_pdf.py 走这条,比拆成两个进程每页省 15% CPU 和一个临时文件)、
  `ocr`(对已有图片文件识别)、`render`(CoreGraphics 精确渲染,瘦身的逐像素校验用它)、`langs`。
  **编译时不能带 `-parse-as-library`** —— 那是主程序 `@main` 用的,命令行工具带上会禁掉顶层语句。
- 语言处理照搬 QuickDict 3 的经验:**拉丁语和 CJK 不能塞进同一个 `recognitionLanguages`**
  (Vision 对 CJK 会直接返回空),要先开 `automaticallyDetectsLanguage`,落空再逐语言重试。
- 隐形字体是 Type0 + Identity-H,**CID 直接等于 Unicode 码位**,配一份恒等 ToUnicode CMap;
  字形不嵌入(反正不绘制)。**别写 `CIDToGIDMap`** —— 没嵌字形时它没意义,写了 CoreGraphics 会抱怨。
- `--sidecar` 顺手导出同名 `.txt`(页间 `\f` 分隔,pdftotext 同款)——终端里 grep 整个书架用。
- **默认跳过已有文字层的页**(内容流里有 BT…ET 内的 Tj/TJ 即算有),对成品重跑 = 全部跳过,
  天然幂等,不会叠两层文字;`--redo` 强制全识别。
- ⚠ **qpdf/pikepdf 不是线程安全的**:MediaBox、parse_content_stream 等一切 pikepdf 访问
  必须在主线程串行做完(见 `metas` 预扫),worker 里只允许跑 subprocess。违反过一次:
  跳过检测随机全部失灵(异常被 except 吞成 False),save 出的文件偶发损坏。
- 局限:仅 macOS;Vision 模型随系统更新变化,故这一项**不像本项目其余部分那样跨机器可复现**。

### `app/ScanToPDF.swift`(1344 行,SwiftUI)

| 类型 | 职责 |
|---|---|
| `Model` (76) | 「图片转 PDF」页的全部设置 + UserDefaults 持久化 + 拼参数(`args`) |
| `Job` (380) | 一个任务:起 `Process`、解析 `@@` 行、进度/日志/耗时/取消 |
| `JobQueue` (501) | 队列与并发(`concurrency` 默认 1),分配 jbig2 名额与每任务 `--jobs` |
| `ConvertView` (564) | 主界面 |
| `MergeModel`/`MergeView` (960/1181) | 「合并 PDF」标签页 |
| `StatusPill`/`JobRow` | 状态胶囊、任务行 |
| `ProbeRow`/`PdfToolsModel`/`PdfToolsView` | 「PDF 工具」页:体检 / 无损瘦身 / 加文字层 / MRC 重制 |
| `RootView`/`ScanToPDFApp` | 三个标签页 + `@main` |

**「PDF 工具」页的几个约定**:
- 体检走 `shrink_pdf.py --analyze --json`,引擎**逐行**吐 `@@J{...}`,GUI 边收边填表(不必等全部跑完)。
- 三种操作的产物后缀必须能一眼分开:`_slim`(无损)、`_ocr`(加文字层)、`_mrc`(**有画质取舍**)。
- 写文件前 `existingOutputs()` 先查存在性再弹窗问,确认后才给引擎传 `--force`(项目铁律,见第 4 节第 6 条)。
- **MRC 是备用路线,用户否掉过**,所以界面上给了橙色警示 + 「样张页」输入框,引导先试几页。
- **加识别语言时要连带迁移老设置**:`langs` 存在 UserDefaults 里,只改默认值的话老用户
  永远拿不到新语言。做法是把历代默认值列在 `legacyLangs`,存的还是旧默认就自动升级,
  用户自己改过的一律不动(同第 5 节那条 `mrcPreset` 的教训,只是这次是字符串)。
  注意 `PdfToolsView` 是懒加载的,迁移在**第一次打开该页时**才发生,不是启动时。
- ⚠ **Picker 别加 `.labelsHidden()`**:在 Form 里会让 radio 跑到行的最右边,很难看;
  重复标题的问题应该改分组标题(现在叫「要做什么」),不是去掉 Picker 的标签。
- ⚠ **别在视图 body 里开 PDF**:`PDFDocument(url:)` 打开一本 584 页的书要 82 ms,而 SwiftUI
  每次状态变化都重算 body——合并列表放几本大书,转换进度每跳一次界面就卡半秒。
  `MergeModel` 的「N 页 · X MB」在 `add()` 时后台算一次存进 `infos` 缓存,body 只读缓存
  (2026-08-05 审核修的)。以后给列表行加任何「文件信息」都走这个模式。

### 两者的接口:`@@` 进度协议(**改任何一边都要同步另一边**)

```
@@P <stage> <done> <total>      进度
@@S <stage>                      阶段切换
@@DONE <bytes> <pages> <path>    完成
```

引擎只在带 `--progress` 时输出这些行(`emit`),GUI 靠它驱动进度条,完成后在 Finder 里定位成品。

### 缓存

`~/.cache/scan2pdf/<源目录名>/<mode>_<width>_<threshold|quality>[_e]/` —— **按档位分开**,
所以换回旧档位重跑只需重组 PDF(秒级)。成功产出后自动删该档位的中间产物;**失败/中断保留**以便续做。
GUI 底部显示残留大小和「清理」按钮。

---

## 4. 不能随手改的东西(改之前先问用户)

1. **统一 A4**:全部页面等比缩放居中到 A4,用户明确要的。
2. **bw 模式用原生分辨率**(`--width 0`):二值化后分辨率几乎不占体积(+35%,绝对值几 MB),放大看笔画明显更实。
3. **大部头(≥300 页)不要开超采样**:1506 页实测 32 MB → 62 MB。GUI 已有橙色提醒 + 「关掉」按钮,别把提醒删了。
4. **MRC 的两个预设是用户实测选定的**:
   - 教材/杂志(**默认**):蒙版 2600 + 底图 1500/q50 + **关**彩色分层
   - 漫画/绘本:蒙版 2200 + 底图 500/q35 + **开**彩色分层
5. **彩色增强的锐化档不要减弱**(`0x0.9+1.0+0.008`)。用户实测:「适中分辨率 + 强锐化」比「高分辨率 + 弱锐化」好看。
   **用户说「不够清晰」时,先查增强/锐化档和阅读器里的实际显示尺寸,不要条件反射去加分辨率**(为此白跑过一版 180 MB)。
6. **给 GUI 加任何写文件的路径时,先查存在性再问**(曾有静默覆盖用户成品的事故)。
7. **文件名输入框必须过 `sanitizeName()`**(曾有剪贴板里的报错文本被粘进文件名)。

---

## 5. 常见维护任务

**改默认档位** → `app/ScanToPDF.swift` 的 `Model` 属性默认值 + `mrcPreset` 的 `didSet`;
引擎侧默认值在 `main()` 的 `add_argument`。两边都要看,GUI 显式传参会盖掉引擎默认值。

**加一个 GUI 选项** → `Model` 加 `@Published`(带 `didSet { save() }`)→ `init` 里恢复 → `save()` 里写入 →
`args` 里拼参数 → 界面加控件。
⚠ **带 `didSet` 的预设属性要写 `guard loaded else { return }`**,否则启动恢复设置时会用预设值盖掉用户微调过的数值;
判断「有没有旧设置」要用**新加的那个键**,否则老用户会拿到「标签是新档、数值是旧档」的错位状态。

**加一个画质模式** → `scan2pdf.py` 写单页函数 → 在 `build()` 里分支 → `--mode` 的 choices 加名字 →
GUI 的模式 Picker 加一项(**慎重**:引擎有 6 个模式,GUI 只放了 4 个——`smooth` 和 `vector` 都是用户看过效果后否掉的,只留命令行)。

**发新版本** → 改 `build_app.py` 顶部 `VERSION` → `python3 app/build_app.py`(自动删同族旧版)→ 打包:

```bash
cd <仓库根目录>/app && rm -rf /tmp/spstage && mkdir -p /tmp/spstage && cp -R "build/ScanPress <版本>.app" /tmp/spstage/ && cp README_安装说明.txt /tmp/spstage/ && ln -s /Applications /tmp/spstage/Applications && hdiutil create -volname "ScanPress <版本>" -srcfolder /tmp/spstage -ov -format UDZO "dist/ScanPress <版本>.dmg" && (cd build && zip -qry "../dist/ScanPress <版本>.zip" "ScanPress <版本>.app") && rm -rf /tmp/spstage
```

记得同步改 `README_安装说明.txt` 里的版本号(**它在 DMG 里面**,改完要重做 DMG),并删掉 `dist/` 里的旧版包。

**动内嵌工具链** → `build_app.py` 的 `bundle_tools()` / `find_modules()` / `sign_inside_out()`,
运行时那头在 `ScanToPDF.swift` 的 `enum Tools`。**四个坑,每个都够卡半天**:

1. **这个 homebrew 的 ImageMagick 是模块化编译的**——coder 是 127 个独立 `.so`(在
   `lib/ImageMagick/modules-Q16HDRI/coders/`,注意目录名是 `ImageMagick` 不是 `ImageMagick-7`)。
   只搬可执行文件和 dylib 的话,连 JPEG 都读不了,报 `no decode delegate`。
2. **模块靠 libltdl 加载,认的是 `.la` 描述文件不是 `.so`**,必须成对复制;而且 `.la` 里的
   `libdir=` 是 homebrew 绝对路径,**不清空成 `libdir=''` 的话,包内的 `.la` 会把 homebrew 那份
   `.so` 拽回来加载**——本机测试一切正常,换台没装 homebrew 的机器立刻失败。这个坑最阴。
3. **`heic.so` 必须排除**:它 → libheif → **x265(GPL-2.0)**,打进 MIT 应用即违约。
   `TAINTED_LIB_PAT` 自动按依赖闭包筛,不用手工维护白名单。potrace 同理,**永远别内嵌**。
4. **带执行位的 `.py` 不能放 `Contents/Helpers/`**:那是「嵌套代码」位置,codesign 会要求单独
   签名(签名写在扩展属性里,打包易丢)。`jbig2topdf.py` 因此放 `Resources/`,由 scan2pdf.py
   按「与自己同目录优先」查找。

改完必须验证:`otool -L` 全包无 `/opt/homebrew` 残留,且用 `env -i PATH=<Helpers>:/usr/bin:/bin`
跑一遍真实转换(见第 6 节第 7 条)。

**动内嵌 Python** → `build_app.py` 的 `prepare_python()` / `bundle_python()`,运行时在 `Tools.python`。
用的是 [python-build-standalone](https://github.com/astral-sh/python-build-standalone) 的可重定位
CPython(版本对齐原来 pdfenv 的 3.14.6),`~/.cache/scanpress/` 缓存**装好包并瘦过身**的整棵树,
所以只有第一次构建要联网,之后是纯拷贝。要点:
- **`lxml` 不能删**(20 MB,看着像能省):`import pikepdf` 时就要它,`--title` 写 dc:title 也走它。
- 删掉的是 pip / ensurepip / idlelib / tkinter / 测试套件 / C 头文件,共 18 MB(126 → 107 MB)。
- 换 Python 版本要连 `PY_VERSION` 和 `PY_RELEASE` 一起改,且确认三个包都有对应的 arm64 wheel。
- Python 的扩展模块是几百个 `.so`,`sign_inside_out()` 用 `macho_files()` **按魔数**递归找出来逐个签
  (漏一个整包签名就报 `code object is not signed at all`)。

**改图标** → `build_app.py` 的 `make_icon()`(magick 画 1024 → sips 出各尺寸 → iconutil 合成)。
本机 magick **没有默认字体**,`-annotate` 必须显式给 `/System/Library/Fonts/*.ttc`。
图标换了但 Finder 还显示旧的,是图标缓存,重登录或 `lsregister -f` 刷新。

---

## 6. 改完的验证清单

0. **先跑自测**:`~/.venvs/pdfenv/bin/python scripts/selftest.py`(约 10 秒,14 项不变量,
   全绿再走下面的人工步骤)。它抓过真 bug:ocr_pdf 曾在 worker 线程里并发访问 pikepdf,
   跳过检测随机失灵——第一次跑 selftest 就红了。
1. `python3 app/build_app.py` 无 error,末行是「已安装」。
2. 包内外 `scan2pdf.py` md5 一致(见第 2 节命令)。
3. `open -a "$HOME/Applications/ScanPress 2.2.app"` 后 `pgrep -x ScanToPDF` 有输出(能启动、没崩)。
4. 跑一本小书(如 116 页的《自然法》源图)全流程:进度条走完、成品能打开、页数对、全是 A4。
5. 动过 MRC 就抽查页面对象数:关分层时内容页 = **底图 + 黑字蒙版**(2 个),开分层时再多几个彩色蒙版;封面封底恒为 1 个(不做 MRC,否则深色画面被涂黑)。
   ⚠ 图层藏在 A4 统一包出来的 **Form XObject 里**,`page.get_images()` 还会跳过 ImageMask——
   要递归穿进 `/Resources//XObject` 数,不然蒙版层数不对也看不出来(2026-08-05 审核就是这么
   发现「关分层却带着 3 个全白彩色层」的:每页白跑 4 次 magick + 3 趟 JBIG2,体积上毫无痕迹,
   60 页 MRC 实测 49.5→40.4 秒,**修掉即提速 18%**)。
6. 动过持久化就测**冷启动**:退出应用重开,设置还在且没被预设盖掉。
7. **动过内嵌工具链就做「无 homebrew 实测」**——这是唯一能证明包是自足的办法(别信本机能跑):

```bash
A="$HOME/Applications/ScanPress 2.2.app"; R="$A/Contents/Resources/ImageMagick"
env -i HOME="$HOME" PATH="$A/Contents/Helpers:/usr/bin:/bin" \
  MAGICK_CONFIGURE_PATH="$R/etc:$R/share:$R/config" \
  MAGICK_CODER_MODULE_PATH="$R/modules/coders" MAGICK_FILTER_MODULE_PATH="$R/modules/filters" \
  "$A/Contents/Resources/python/bin/python3" "$A/Contents/Resources/scan2pdf.py" \
  <源图目录> /tmp/t.pdf --mode mrc --title 自足测试
```

   `env -i` + 包内 python + 包内工具链 = 模拟一台只装了本应用的干净机器。**别用 pdfenv 的 python 测**,
   那样测不出包内 Python 有没有问题。`--title` 要带上,它是唯一会走 lxml 的路径。

   再把产出与 homebrew 版逐个图像流比 md5(`pikepdf` 打开 → `page.get_images()` → `read_raw_bytes()`;
   **注意页面内容被包在 Form XObject 里,`page.images` 取不到,必须用 `get_images()`**)。
   2026-08-03 首次内嵌时 bw / mrc 两种模式各 20 页,全部图像流逐字节一致。

---

## 7. 已试过并否掉的路线(别重复踩)

| 路线 | 结果 |
|---|---|
| 局部自适应阈值 `-lat` | 糊肿填死字腔、体积翻倍;CZUR 打光均匀,全局阈值够用 |
| 锐化来减轻二值化锯齿 | **无效**,粗糙度反而 +0.8%;锯齿是 1-bit 量化的必然,只能靠灰阶或超采样 |
| Photoshop 式「平滑」位图 | 实为灰度+色阶,对高对比扫描件无改善 |
| potrace 矢量 | 能做且分辨率无关,但 2.7 MB vs `bw --enhance --supersample 2` 的 0.63 MB,用户看过否掉;GPL-2.0 也挡商用 |
| 矢量与黑白「合并版」 | 用户:难看,不要 |
| JPEG2000 | 同画质点体积 104% of JPEG,还有阅读器兼容风险 |
| MRC 结构检测(顶帽/底帽)取代颜色判定 | 文字全抓到了,但 RGB k-means 只聚出中性簇,深红文字被画成灰色,体积还翻倍 |
| 彩色字分层用于彩色文字密集的教材 | 笔画被劈在锐利蒙版层和模糊底图之间 → 「像被虫子啃了」,形态学修补无效,**只能关掉分层** |
| **用 MRC 重制已有的彩色 PDF**(`shrink_pdf.py --mrc`) | 技术上完全成功:167 MB 教材 → 约 21 MB(0.197×),文字层还从 4960px JPEG 升成 2600px 黑白蒙版,原 OCR 文字层也保住了。但**用户看过样张后否掉:「还是觉得原图好看」**(2026-08-03)。代码保留在命令行,**不进 GUI、不做默认**,同 smooth/vector 的待遇 |
| 无损 JPEG 重编码(jpegtran -optimize -progressive)给彩色页瘦身 | 确认真无损(逐像素一致),但**只省 4.3%**(167.3 → 160 MB),不值得做成功能 |
| 提速方向:`MAGICK_THREAD_LIMIT=1`、PNG 压缩级别、PBM 中间格式、`-filter Triangle` | 全都没用或更慢;剩余唯一杠杆是超采样倍数 |

---

## 8. 环境依赖

- `/opt/homebrew/bin`:`magick`(ImageMagick 7)、`jbig2`(jbig2enc)+ `jbig2topdf.py`、`potrace`(仅矢量模式)
- `~/.venvs/pdfenv`(pikepdf / img2pdf / Pillow)。重建:
  ```bash
  python3 -m venv ~/.venvs/pdfenv && ~/.venvs/pdfenv/bin/pip install pikepdf reportlab img2pdf
  ```
- 构建只需 **Xcode Command Line Tools**,不需完整 Xcode。swiftc 必须带 `-parse-as-library`(否则 `@main` 报错)和 `-swift-version 5`(规避 Swift 6 并发严格检查)。
- **应用没有内嵌依赖**,只能在装好这套 homebrew 工具链的机器上跑;启动时会自检并用橙色面板提示缺什么。
- **`jbig2`/leptonica 会重映射 `/tmp/` 开头的绝对路径**(报 `fopenReadStream: failed to open locally with tail …`,文件明明在)。调它一律 `cwd=工作目录` + 相对路径。
- macOS 系统 bash 是 3.2,编排逻辑一律用 python,别写非平凡 shell 脚本。

---

## 9. 待办 / 悬着的事

- **Windows 版没做**。用户打算在 Windows 机器上开新会话按 `app/WINDOWS_移植交接.md` 做。核心改动只有三处:`fcntl`→`msvcrt`、缓存目录改 `%LOCALAPPDATA%`、命令一律用 `magick` 不用 `convert`;JBIG2 在 Windows 上没有现成二进制,退路是 CCITT G4。
- **商业化**:评估过,结论偏负面(依赖 homebrew 工具链、potrace 的 GPL、竞品免费),用户没再提。
- 用户说「后期若遇到新的难题再找你调整」——**等他报问题**,别主动改画质参数。

---

## 10. 与书籍项目的关系

作者本机还有**另一个私有项目**(未开源):同样是扫描图片进,但产出
「Claude 视觉逐页转录的 Word + 带隐形文字层的可搜索 PDF」。本项目是它的**纯确定性旁路**,2026-08-03 才拆出来独立。

共享的技术:二值化配方(阈值 55% + 边缘涂白)、切段 JBIG2、统一 A4 的 pikepdf 矢量缩放法。
**一边改了这几处,另一边留意同步。** 其余互不相干,别把 book.json 那套配置驱动的思路带进来——本项目**刻意零配置**。
