# ScanPress 维护交接

写给「以后开新会话来改这个软件」的自己。**先读本文,再读 `CLAUDE.md`**:
本文讲**怎么动手**(代码在哪、怎么构建验证、哪些不能动),`CLAUDE.md` 讲**为什么是现在这样**(全部实测数据与踩坑)。

---

## 1. 现状(2026-08-03)

| 项 | 值 |
|---|---|
| 版本 | **2.1**(应用名 `ScanPress 2.1`,版本号在 `app/build_app.py` 顶部 `VERSION` 常量,一处改全处生效) |
| 已安装 | `~/Applications/ScanPress 2.1.app` |
| 分发包 | `app/dist/ScanPress 2.1.dmg`(1.0 MB)、`.zip`(953 KB) |
| Windows 版 | **未做**。交接文档 `app/WINDOWS_移植交接.md` + `app/dist/ScanPress-Windows移植包.zip`,方案是「在 Windows 上单独写一个界面、复用同一份 scan2pdf.py」 |
| 用户 | 就是本仓库主人。用扫描仪(CZUR)拍整本书,要**高清晰 + 小体积**的 PDF。日常从图形界面用,极少用命令行 |

**用户对当前效果是满意的**(2026-08-03 原话:「文字变清了很多,似乎是够用的。我先这么用着,后期若遇到新的难题再找你调整。」)。
所以默认心态是:**别主动优化画质参数**,等用户报问题再动。

---

## 2. 三分钟上手(改代码 → 构建 → 验证)

```bash
cd <仓库根目录>
# 改 scripts/scan2pdf.py 或 app/ScanToPDF.swift 之后:
pkill -x ScanToPDF          # 应用在跑的话先退,否则覆盖安装不干净
python3 app/build_app.py    # 编译 swift → 组装 .app → 复制脚本 → 签名 → 装到 ~/Applications
```

**最容易犯的错:改了 `scripts/scan2pdf.py` 却没重跑 `build_app.py`。** 应用跑的是 `.app` 包内的拷贝,
源文件改了它一无所知。每次改完脚本用这条确认包内外一致:

```bash
md5 -q scripts/scan2pdf.py "$HOME/Applications/ScanPress 2.1.app/Contents/Resources/scan2pdf.py"
```

只测引擎不用构建应用:

```bash
~/.venvs/pdfenv/bin/python scripts/scan2pdf.py <源图目录> /tmp/out.pdf --mode bw --enhance --supersample 2
```

`--probe` 可以只抽 8 页试多个档位,**定档不必全书重跑**——调参时永远先用它。

---

## 3. 代码地图

### `scripts/scan2pdf.py`(890 行,全部转换逻辑;零配置、不读 book.json)

| 位置 | 职责 | 备注 |
|---|---|---|
| `emit/die/run/fresh` | 进度输出、报错、跑外部命令 | `emit` 就是 `@@` 协议的出口 |
| `parse_ranges` `list_images` `img_size` | 选页、自然排序、读宽高 | `img_size` 用 Pillow,**别改回 `magick identify`**(每页贵 43 ms) |
| `binarize` (132) | bw 模式:缩放 → 增强 → 超采样 → 阈值 → 边缘涂白 | 边缘涂白 = 左右各 1.5%、上下各 1%,去书脊阴影 |
| `smooth_bw` (155) | smooth 模式:4× 阈值 → 形态学平滑 → 降采样 → 3 级灰 | **GUI 不暴露**;用户实测觉得比 bw 更糊,故没进界面 |
| `vectorize` (184) | vector 模式:potrace 描摹 | **GUI 不暴露**,仅命令行;`zoom` 必须 ≥2 |
| `mrc_layers` (228) | MRC 拆层:黑字蒙版 / 彩色字蒙版 / 挖墨后的彩色底图 | **全文件最难的函数**,改前必读函数内注释块 |
| `mrc_compose` (390) | 把各层压 JBIG2 并组成 PDF 页 | 每层一个 JBIG2 批次以共享词典 |
| `to_jpeg` (440) | gray/color 模式出图 | 增强配方按模式分档 |
| `pmap` (454) | 多进程跑单页任务 | |
| `jbig2_slot` (473) | **跨进程 flock 名额**(`~/.cache/scan2pdf/.slots/`,8 个) | 单任务独占 8 个,中途加任务自动分摊,无需重启 |
| `jbig2_pdf` (501) | **切段并行 JBIG2** | 切段本身就是 11.6× 提速(词典变小),并行只再给 2.6×,故**任何情况下都切段** |
| `assemble` (564) | 组页 + 统一 A4(pikepdf 矢量缩放) | |
| `build` (591) | 一次转换的总编排 | 断点续做、缓存清理都在这 |
| `main` (739) | 参数解析 | |

### `app/ScanToPDF.swift`(1344 行,SwiftUI)

| 类型 | 职责 |
|---|---|
| `Model` (76) | 「图片转 PDF」页的全部设置 + UserDefaults 持久化 + 拼参数(`args`) |
| `Job` (380) | 一个任务:起 `Process`、解析 `@@` 行、进度/日志/耗时/取消 |
| `JobQueue` (501) | 队列与并发(`concurrency` 默认 1),分配 jbig2 名额与每任务 `--jobs` |
| `ConvertView` (564) | 主界面 |
| `MergeModel`/`MergeView` (960/1181) | 「合并 PDF」标签页 |
| `StatusPill`/`JobRow` | 状态胶囊、任务行 |
| `RootView`/`ScanToPDFApp` | 两个标签页 + `@main` |

### 两者的接口:`@@` 进度协议(**改任何一边都要同步另一边**)

```
@@P <stage> <done> <total>      进度
@@S <stage>                      阶段切换
@@DONE <bytes> <pages> <path>    完成
```

引擎只在带 `--progress` 时输出这些行(`emit`),GUI 靠它驱动进度条,完成后在 Finder 里定位成品。

### 缓存

`~/.cache/scan2pdf/<源目录名>/<mode>_<width>_<threshold|quality>[_e]/` —— **按档位分开**,
所以换回旧档位重跑只需重组 PDF(秒级)。成功产出后自动删该档位的中间产物;**失败/中断保留**以便续做。
GUI 底部显示残留大小和「清理」按钮。

---

## 4. 不能随手改的东西(改之前先问用户)

1. **统一 A4**:全部页面等比缩放居中到 A4,用户明确要的。
2. **bw 模式用原生分辨率**(`--width 0`):二值化后分辨率几乎不占体积(+35%,绝对值几 MB),放大看笔画明显更实。
3. **大部头(≥300 页)不要开超采样**:1506 页实测 32 MB → 62 MB。GUI 已有橙色提醒 + 「关掉」按钮,别把提醒删了。
4. **MRC 的两个预设是用户实测选定的**:
   - 教材/杂志(**默认**):蒙版 2600 + 底图 1500/q50 + **关**彩色分层
   - 漫画/绘本:蒙版 2200 + 底图 500/q35 + **开**彩色分层
5. **彩色增强的锐化档不要减弱**(`0x0.9+1.0+0.008`)。用户实测:「适中分辨率 + 强锐化」比「高分辨率 + 弱锐化」好看。
   **用户说「不够清晰」时,先查增强/锐化档和阅读器里的实际显示尺寸,不要条件反射去加分辨率**(为此白跑过一版 180 MB)。
6. **给 GUI 加任何写文件的路径时,先查存在性再问**(曾有静默覆盖用户成品的事故)。
7. **文件名输入框必须过 `sanitizeName()`**(曾有剪贴板里的报错文本被粘进文件名)。

---

## 5. 常见维护任务

**改默认档位** → `app/ScanToPDF.swift` 的 `Model` 属性默认值 + `mrcPreset` 的 `didSet`;
引擎侧默认值在 `main()` 的 `add_argument`。两边都要看,GUI 显式传参会盖掉引擎默认值。

**加一个 GUI 选项** → `Model` 加 `@Published`(带 `didSet { save() }`)→ `init` 里恢复 → `save()` 里写入 →
`args` 里拼参数 → 界面加控件。
⚠ **带 `didSet` 的预设属性要写 `guard loaded else { return }`**,否则启动恢复设置时会用预设值盖掉用户微调过的数值;
判断「有没有旧设置」要用**新加的那个键**,否则老用户会拿到「标签是新档、数值是旧档」的错位状态。

**加一个画质模式** → `scan2pdf.py` 写单页函数 → 在 `build()` 里分支 → `--mode` 的 choices 加名字 →
GUI 的模式 Picker 加一项(**慎重**:引擎有 6 个模式,GUI 只放了 4 个——`smooth` 和 `vector` 都是用户看过效果后否掉的,只留命令行)。

**发新版本** → 改 `build_app.py` 顶部 `VERSION` → `python3 app/build_app.py`(自动删同族旧版)→ 打包:

```bash
cd <仓库根目录>/app && rm -rf /tmp/spstage && mkdir -p /tmp/spstage && cp -R "build/ScanPress <版本>.app" /tmp/spstage/ && cp README_安装说明.txt /tmp/spstage/ && ln -s /Applications /tmp/spstage/Applications && hdiutil create -volname "ScanPress <版本>" -srcfolder /tmp/spstage -ov -format UDZO "dist/ScanPress <版本>.dmg" && (cd build && zip -qry "../dist/ScanPress <版本>.zip" "ScanPress <版本>.app") && rm -rf /tmp/spstage
```

记得同步改 `README_安装说明.txt` 里的版本号(**它在 DMG 里面**,改完要重做 DMG),并删掉 `dist/` 里的旧版包。

**改图标** → `build_app.py` 的 `make_icon()`(magick 画 1024 → sips 出各尺寸 → iconutil 合成)。
本机 magick **没有默认字体**,`-annotate` 必须显式给 `/System/Library/Fonts/*.ttc`。
图标换了但 Finder 还显示旧的,是图标缓存,重登录或 `lsregister -f` 刷新。

---

## 6. 改完的验证清单

1. `python3 app/build_app.py` 无 error,末行是「已安装」。
2. 包内外 `scan2pdf.py` md5 一致(见第 2 节命令)。
3. `open -a "$HOME/Applications/ScanPress 2.1.app"` 后 `pgrep -x ScanToPDF` 有输出(能启动、没崩)。
4. 跑一本小书(如 116 页的《自然法》源图)全流程:进度条走完、成品能打开、页数对、全是 A4。
5. 动过 MRC 就抽查页面对象数:关分层时内容页 = **底图 + 黑字蒙版**(2 个),开分层时再多几个彩色蒙版;封面封底恒为 1 个(不做 MRC,否则深色画面被涂黑)。
6. 动过持久化就测**冷启动**:退出应用重开,设置还在且没被预设盖掉。

---

## 7. 已试过并否掉的路线(别重复踩)

| 路线 | 结果 |
|---|---|
| 局部自适应阈值 `-lat` | 糊肿填死字腔、体积翻倍;CZUR 打光均匀,全局阈值够用 |
| 锐化来减轻二值化锯齿 | **无效**,粗糙度反而 +0.8%;锯齿是 1-bit 量化的必然,只能靠灰阶或超采样 |
| Photoshop 式「平滑」位图 | 实为灰度+色阶,对高对比扫描件无改善 |
| potrace 矢量 | 能做且分辨率无关,但 2.7 MB vs `bw --enhance --supersample 2` 的 0.63 MB,用户看过否掉;GPL-2.0 也挡商用 |
| 矢量与黑白「合并版」 | 用户:难看,不要 |
| JPEG2000 | 同画质点体积 104% of JPEG,还有阅读器兼容风险 |
| MRC 结构检测(顶帽/底帽)取代颜色判定 | 文字全抓到了,但 RGB k-means 只聚出中性簇,深红文字被画成灰色,体积还翻倍 |
| 彩色字分层用于彩色文字密集的教材 | 笔画被劈在锐利蒙版层和模糊底图之间 → 「像被虫子啃了」,形态学修补无效,**只能关掉分层** |
| 提速方向:`MAGICK_THREAD_LIMIT=1`、PNG 压缩级别、PBM 中间格式、`-filter Triangle` | 全都没用或更慢;剩余唯一杠杆是超采样倍数 |

---

## 8. 环境依赖

- `/opt/homebrew/bin`:`magick`(ImageMagick 7)、`jbig2`(jbig2enc)+ `jbig2topdf.py`、`potrace`(仅矢量模式)
- `~/.venvs/pdfenv`(pikepdf / img2pdf / Pillow)。重建:
  ```bash
  python3 -m venv ~/.venvs/pdfenv && ~/.venvs/pdfenv/bin/pip install pikepdf reportlab img2pdf
  ```
- 构建只需 **Xcode Command Line Tools**,不需完整 Xcode。swiftc 必须带 `-parse-as-library`(否则 `@main` 报错)和 `-swift-version 5`(规避 Swift 6 并发严格检查)。
- **应用没有内嵌依赖**,只能在装好这套 homebrew 工具链的机器上跑;启动时会自检并用橙色面板提示缺什么。
- **`jbig2`/leptonica 会重映射 `/tmp/` 开头的绝对路径**(报 `fopenReadStream: failed to open locally with tail …`,文件明明在)。调它一律 `cwd=工作目录` + 相对路径。
- macOS 系统 bash 是 3.2,编排逻辑一律用 python,别写非平凡 shell 脚本。

---

## 9. 待办 / 悬着的事

- **Windows 版没做**。用户打算在 Windows 机器上开新会话按 `app/WINDOWS_移植交接.md` 做。核心改动只有三处:`fcntl`→`msvcrt`、缓存目录改 `%LOCALAPPDATA%`、命令一律用 `magick` 不用 `convert`;JBIG2 在 Windows 上没有现成二进制,退路是 CCITT G4。
- **商业化**:评估过,结论偏负面(依赖 homebrew 工具链、potrace 的 GPL、竞品免费),用户没再提。
- 用户说「后期若遇到新的难题再找你调整」——**等他报问题**,别主动改画质参数。

---

## 10. 与书籍项目的关系

作者本机还有**另一个私有项目**(未开源):同样是扫描图片进,但产出
「Claude 视觉逐页转录的 Word + 带隐形文字层的可搜索 PDF」。本项目是它的**纯确定性旁路**,2026-08-03 才拆出来独立。

共享的技术:二值化配方(阈值 55% + 边缘涂白)、切段 JBIG2、统一 A4 的 pikepdf 矢量缩放法。
**一边改了这几处,另一边留意同步。** 其余互不相干,别把 book.json 那套配置驱动的思路带进来——本项目**刻意零配置**。
